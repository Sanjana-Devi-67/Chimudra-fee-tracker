<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chinmudra Academy for Performing Arts - Login</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Roboto:wght@300;400;500&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-red: #B31312;
            --white: #ffffff;
        }
        body {
            font-family: 'Roboto', sans-serif;
        }
        .heading {
            font-family: 'Playfair Display', serif;
        }
        .mandala-bg {
            background-image: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><circle cx="50" cy="50" r="45" fill="none" stroke="%23B31312" stroke-width="0.5" opacity="0.1"/></svg>');
            background-size: 200px;
        }
    </style>
</head>
<body class="bg-white mandala-bg min-h-screen">
    <%- include('../partials/navbar') %>
    <h1 class="text-3xl font-bold mb-6 text-center text-red-600 mt-7">Payment History</h1>

  <div class="max-w-4xl mx-auto bg-white p-6 rounded-lg shadow">
    <% if (payments.length > 0) { %>
      <table class="w-full text-left border border-gray-300">
        <thead class="bg-red-600 text-white">
          <tr>
            <th class="p-2">Amount</th>
            <th class="p-2">Description</th>
            <th class="p-2">Status</th>
            <th class="p-2">Date</th>
          </tr>
        </thead>
        <tbody>
          <% payments.forEach(payment => { %>
            <tr class="border-t border-gray-300">
              <td class="p-2">₹ <%= payment.amount %></td>
              <td class="p-2"><%= payment.description %></td>
              <td class="p-2"><%= payment.status %></td>
              <td class="p-2"><%= payment.createdAt ? payment.createdAt.toDateString() : 'N/A' %></td>
            </tr>
          <% }); %>
        </tbody>
      </table>
    <% } else { %>
      <p class="text-center text-gray-600">No payment history found.</p>
    <% } %>
  </div>
</body>
</html>